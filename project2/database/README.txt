For the database to work, you must move the project into ".xampp/htdocs".
Then move the actual database itself (the folder next to this) into ".xampp/mysql/data".
(I have no idea if this will actually work it would be nice if someone could test it)